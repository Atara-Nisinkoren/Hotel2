@model Dictionary<HotelProject.Models.RoomType, int>

@{
    ViewData["Title"] = "SearchResults";
}
<form asp-action="RedirectToPayment">
    @foreach (var room in Model)
    {

        <div class="card mb-3 mbb">
            <div class="row no-gutters">
                <div class="col-md-6">
                    <img src="../@room.Key.ImgUrl" class="card-img imgSize" alt="...">
                </div>
                <div class="col-md-6">
                    <div class="card-body">
                        <h2 class="card-title right">@room.Key.Name</h2>
                        <p class="card-text right fsize roomDes"></p>
                        <h5>
                            <p class="right white">
                                @room.Key.Desc
                            </p>
                        </h5>
                        <div id="moreDetailes">
                            <hr />
                            <dl class="row hebrew">
                                <dt class="col-sm-6 white">
                                    מספר חדרים להזמנה:
                                </dt>
                                <dd class="col-sm-2 white">
                                    @room.Value
                                </dd>
                                <dt class="col-sm-6 white">
                                    מחיר כולל:
                                </dt>
                                <dd class="col-sm-2 white">
                                    @String.Format("{0:0,0}", @room.Key.BasicPrice)
                                </dd>
                            </dl>
                        </div>
                        <br />



                        <a asp-controller="Home" asp-action="RedirectToPayment" asp-route-id="@room.Key.Id" type="submit" class="btn btn-outline-warning">
                            הזמן
                            עכשיו
                        </a>

                    </div>
                </div>
            </div>
        </div>
    }
</form>




@section Css{
    <link rel="stylesheet" href="~/css/searchResults.css" />
}